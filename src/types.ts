export type BallParams = {
    size: number;
    color: string;
    restitution: number;
    mass: number;
    fixed: boolean;
};

export type BoxParams = {
    width: number;
    height: number;
    color: string;
    restitution: number;
}